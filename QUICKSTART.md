# 🚀 Быстрый старт

## Шаг 1: Установка pnpm (если еще не установлен)

```bash
npm install -g pnpm
```

## Шаг 2: Установка зависимостей проекта

```bash
cd "c:\Users\amang\OneDrive\Desktop\global optima"
pnpm install
```

## Шаг 3: Инициализация MSW (Mock Service Worker)

```bash
npx msw init public/ --save
```

Это создаст файл `public/mockServiceWorker.js`, необходимый для работы моков API.

## Шаг 4: Запуск dev-сервера

```bash
pnpm dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

---

## Альтернативный вариант: с npm

Если не хотите устанавливать pnpm, можно использовать npm:

```bash
# Установка зависимостей
npm install

# Инициализация MSW
npx msw init public/ --save

# Запуск dev-сервера
npm run dev
```

---

## Дополнительные команды

### Запуск тестов
```bash
pnpm test
```

### Запуск тестов с UI
```bash
pnpm test:ui
```

### Сборка для production
```bash
pnpm build
```

### Просмотр production сборки
```bash
pnpm preview
```

### Линтинг кода
```bash
pnpm lint
```

---

## ⚠️ Важно

1. **MSW работает только в development режиме** - моки API не будут работать в production сборке
2. После первого запуска может потребоваться разрешить MSW worker в браузере
3. Если возникают ошибки, убедитесь что:
   - Node.js версии 18+ установлен
   - Все зависимости установлены (`pnpm install`)
   - MSW инициализирован (`npx msw init public/ --save`)

---

## 🎯 Что дальше?

После запуска вы увидите:
- **Главную страницу** с каталогом продуктов
- **Фильтры и поиск** вверху страницы
- **Корзину** (иконка в header)
- **Checkout** (4 шага оформления заказа)

Удачи! 🚀


